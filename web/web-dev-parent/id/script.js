// In JavaScript, you can use the getElementById() method to access an element with a specific id.

// Get the element with the id "my-heading"
const heading = document.getElementById('my-heading');

// Change its text content
heading.textContent = 'New Heading with ID';
