// You can also manipulate iframes using JavaScript.
// For example, you can change the src attribute.

// Get the iframe element
const iframe = document.querySelector('iframe');

// Change its src attribute
// Note: Cross-origin security restrictions might prevent you from interacting with the content of the iframe.
// iframe.src = 'https://www.bing.com';
